# 个人主页 · 操作手册

> 项目位置:`~/personal-homepage/` · 本地访问:http://localhost:5173/personal-homepage/(直接访问 5173 会自动跳转)
> 本文档记录主页的全部重要步骤 —— 已完成的历史 + 以后要做的操作。
> **维护约定(2026-09-17 起):以后凡是主页相关的改动(不论是谁做的),主要步骤都会补录到本手册的「时间线」和「版本表」里,并随 git 一起提交。**

---

## 一、已完成的重要步骤(时间线)

| 日期 | 完成事项 |
|---|---|
| 09-06 | **项目初始化**:Vite + React 19 + TypeScript + Tailwind CSS v4 + framer-motion + lucide-react,玻璃拟态设计(蓝紫配色) |
| 09-06 | **本地跑通**:`npm install` / `npm run dev` / `npm run build` 全部验证通过 |
| 09-06 | **深色 → 浅色主题**:浅蓝白底(#F5F7FD)、白色磨砂玻璃卡片、深色文字 |
| 09-06 | **填入真实信息**:姓名 何储安 / 天津大学香港理工大学深圳未来技术学院 / 智能医学工程 / bio / 邮箱 / 微信号(修复了两处手改语法错误) |
| 09-10 | **全站中文化**:所有英文文案换中文(专有名词 C++ / Python / Linux / GitHub / Discord 保留) |
| 09-10 | **换头像**:桌面「照片.jpg」→ `public/avatar.jpg`,圆形裁切 + 发光旋转环 |
| 09-10 | **神经元背景动画**:Canvas 脑机接口风格(神经元漂移、突触连线、信号脉冲、鼠标推开),已调大调柔、放慢脉冲 |
| 09-10 | **名字星光效果**:SparklesText 组件(16 颗四角星,紫青渐变,循环闪烁) |
| 09-10 | **背景升级 3D**:「神经元教堂」球面点云(Three.js + simplex 噪声位移,点击触发神经冲动),由你提供的 Neural Cathedral demo 浅色适配而来;同日按反馈改为**有机形态**:稀疏的细胞体簇 + 突触连线 + 轴突延伸,更接近真实神经元;2D 旧版保留在 `NeuralBackground.tsx` 备用 |
| 09-10 | **桌面快捷方式**:`~/Desktop/打开主页.command`,双击即启动并打开浏览器 |
| 09-10 | **尝试过临时分享链接**(localtunnel,已失效):结论 —— 重启电脑后失效,仅适合临时演示,正式分享要走下面的部署流程 |
| 09-17 | **留言板功能**:先做 FormSubmit 邮箱版(版本6),同日升级为 **Supabase 后台版**(版本7):反馈存数据库,含称呼/关系/设备字段;并完成 **GitHub Pages 部署准备**(base 路径、头像路径适配、`.env.local` 环境变量) |
| 09-24 | **爱好卡片调整**:「生活与运动」改为「生活与爱好」,删除城市骑行和健身,只保留摄影(图标与文案同步);桌面启动脚本和手册中的本地地址更新为 `/personal-homepage/` 路径 |
| 09-24 | **摄影作品上墙**:生活与爱好卡片加入 4 张摄影作品(缩略图网格 + 点击大图查看,支持左右切换/ESC 关闭);原图已压缩为 1400px 宽存入 `public/photos/` |
| 09-24 | **删除「游戏与科技」卡片**:个人爱好区只保留音乐、阅读、生活与爱好三张卡;生活与爱好扩为整行,摄影墙桌面端改为一行四列 |
| 09-24 | **摄影墙微调**:照片整体收窄并居中(最大宽 768px),间距加大,新增「点击照片查看大图」提示,与卡片比例更协调 |
| 09-24 | **摄影墙改为横滑照片条**:一行排列可左右滑动(吸附 + 隐藏滚动条 + 两侧箭头),点击看大图保留 |
| 09-24 | **音乐卡片接入网易云**:移除假"正在播放"组件,嵌入网易云外链播放器;歌曲 ID 配置在 `data.ts` 的 `neteaseSongId`(当前为占位符待填),附「在网易云打开」兜底链接 |
| 09-24 | **填入歌曲《雪人》驼儿**(ID 2685515657):注意该歌曲为付费/VIP(fee:8),外链播放器可能受版权限制无法播放,如遇此情况换免费歌曲即可 |
| 09-24 | **数字分身「小何」上线**:右下角悬浮聊天窗口,基于关键词规则自动回答访客问题(专业/微信/邮箱/爱好等),带打字动画与快捷问题,无需后端 |
| 09-24 | **生成开发报告**:按课程要求产出《个人主页开发报告》PDF(13 页:版本历史与截图 / 功能及想法来源 / 问题与解决 / 反馈与决策),存放于 `~/Desktop/主页开发报告/`,含 3 张真实截图 |
| 09-24 | **回退到版本17**:撤销「我的日常」板块(git revert,可逆操作);等日常素材齐备后再重新制作 |

---

## 二、以后的重要步骤

### A. 日常步骤(每次开机 / 每次修改)

1. **开机后**:双击桌面「打开主页.command」→ 自动启动服务器并打开浏览器
   - 备用方式:终端运行 `cd ~/personal-homepage && npm run dev`
2. **修改个人信息**:编辑 `src/data.ts`(姓名、学校、bio、邮箱、社交链接、微信号全在这)
   - 保存后页面**自动热更新**,无需重启
3. **换头像**:把新照片改名为 `avatar.jpg`,覆盖 `public/avatar.jpg`,刷新即可
4. **停止服务器**:终端运行 `pkill -f vite`
5. **准备发布前**:在项目目录运行 `npm run build` → 生成最新 `dist/` 产物

### A2. 保存版本(每次改完都存一版,随时可回退)

- **最简单**:双击桌面「保存版本.command」→ 自动把当前改动存成一个新版本,并列出最近 5 个版本
- Claude 帮你改主页时,每次改完也会自动提交一个版本
- **查看所有版本**:终端 `cd ~/personal-homepage && git log --oneline`
- **恢复到旧版本**:告诉 Claude「恢复到第 N 版」,或用命令 `git checkout <版本号> -- 文件名` 恢复单个文件
- 已保存的版本:

| 版本 | 内容 |
|---|---|
| 版本1(2805b52) | 初始版本:浅色玻璃拟态 + 神经元背景 + 星光名字 |
| 版本2(9e3a548) | 精简联系方式,只保留微信和邮箱 |
| 版本3(f1bf074) | 手册增加保存版本说明 |
| 版本4(dd612e2) | 背景升级为 3D 神经元教堂(Three.js) |
| 版本5(cb1c483) | 神经元背景改为稀疏有机形态(细胞体簇 + 突触连线) |
| 版本6(368e24d) | 新增留言板(FormSubmit 转发到邮箱) |
| 版本7(698089c) | 接入反馈后台(Supabase)+ 配置 GitHub Pages |
| 8391b30 | 修复 GitHub Pages 头像路径 |
| 版本8(727e6ad) | 手册补齐 Supabase 反馈板与 GitHub Pages 部署说明 |
| b57e1df | 确立手册维护约定(主页改动一律补录) |
| 版本9(abefef3) | 生活与运动改为生活与爱好,只保留摄影 |
| 版本10(e00dd68) | 摄影作品上墙(4 张照片 + 大图查看) |
| 版本11(ceb4859) | 删除游戏与科技卡片,爱好区重排 |
| 版本12(52c0f48) | 摄影墙收窄居中,加查看提示 |
| 版本13(deaee86) | 摄影墙改为一行横滑照片条 |
| 版本14 | 音乐卡片嵌入网易云播放器 |
| 版本15 | 播放器填入歌曲《雪人》驼儿 |
| 版本16(5396dc9) | 数字分身「小何」上线 |
| 版本17 | 生成开发报告(桌面「主页开发报告」文件夹) |
| 版本19 | 回退到版本17状态(移除「我的日常」,可逆) |

### B. 上交流程(发布到公网,让别人能访问)

> 核心概念:你电脑上的页面只有自己能看到;"上传"就是把 `dist/` 产物放到公网服务器上。

#### 方案一:Netlify Drop(最简单,推荐先试,无需注册也能用)

1. 终端:`cd ~/personal-homepage && npm run build`
2. 浏览器打开 **app.netlify.com/drop**
3. 访达 `Cmd + Shift + G` 输入 `/Users/haochia/personal-homepage/dist`,回车
4. 把整个 `dist` 文件夹**拖进网页** → 立即得到永久链接 `https://xxx.netlify.app`
5. **以后更新内容**:重新 build → 再拖一次(注册账号后可绑定固定地址,更新更省事)

#### 方案二:GitHub Pages(需要 GitHub 账号,国内访问稍稳)

```bash
cd ~/personal-homepage
git init                                  # ✅ 已帮你执行
git add .
git commit -m "first commit"
git remote add origin https://github.com/你的用户名/personal-homepage.git
git push -u origin main
```

- GitHub 仓库 → Settings → Pages → 选 main 分支 → 得到 `https://用户名.github.io/personal-homepage/`
- ✅ `vite.config.ts` 已配置 `base: '/personal-homepage/'`,头像路径也已适配(版本7 完成),直接推送即可,无需再改
- **以后更新**:改完内容后 `git add . && git commit -m "update" && git push`,GitHub 自动重新部署

#### 方案三:Vercel(最省心,更新全自动)

1. 先把代码推到 GitHub(按方案二的前 5 行命令)
2. 打开 vercel.com,用 GitHub 账号登录
3. 「Add New Project」→ 选择 `personal-homepage` 仓库 → 自动识别 Vite,直接 Deploy
4. 得到 `https://xxx.vercel.app` 永久链接
5. **以后更新**:只需 `git push`,Vercel 自动重新构建部署,零操作

#### 方案对比

| | Netlify Drop | GitHub Pages | Vercel |
|---|---|---|---|
| 注册门槛 | 无(推荐先试) | 需 GitHub 账号 | 需账号 |
| 更新方式 | 手动拖拽 | git push | git push 全自动 |
| 国内访问 | 一般 | 稍稳 | 一般 |

> 长期面向国内访问,可考虑腾讯云/阿里云静态托管(需实名 + 备案),需要时找 Claude 帮忙。

### C. 常见问题

| 问题 | 解决 |
|---|---|
| 打开 localhost:5173 没反应 | 终端运行 `cd ~/personal-homepage && npm run dev` |
| 提示端口被占用 | 先运行 `pkill -f vite`,再重新启动 |
| 临时分享链接打不开 | 重启电脑/关闭会话后即失效,正式分享请用上面的部署方案 |
| 修改后页面没变化 | 浏览器强制刷新 `Cmd + Shift + R`;确认文件保存在正确位置 |
| 想回滚到之前的样子 | 用 `git diff` / `git checkout` 找回(推 GitHub 前先 commit 一版) |

### D. 反馈板(接收反馈)

- 访客在主页底部「给我反馈」填写(称呼、与我的关系、使用设备、反馈内容),提交后数据存入 **Supabase** 数据库的 `feedback` 表,带有版本号字段
- **查看反馈**:登录 [supabase.com](https://supabase.com) → 进入你的项目 → Table Editor → `feedback` 表(反馈不会公开,只有你能看到)
- **配置密钥**:项目根目录 `.env.local` 存放 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_PUBLISHABLE_KEY`(已被 .gitignore 忽略,不会上传;**换电脑或重新部署时需重新创建此文件**,密钥可在 Supabase 后台 → Settings → API 找到)
- 历史:版本6 曾用 FormSubmit 邮箱方案,版本7 起替换为 Supabase 方案

### E. 数字分身「小何」

- 主页右下角悬浮按钮,点击打开聊天窗口;基于**关键词规则**自动回答(学校/专业/微信/邮箱/爱好/留言等),带打字动画和快捷问题
- **修改答案**:编辑 `src/components/DigitalTwin.tsx` 里的 `answer()` 函数(增加新的关键词分支即可)
- **无需后端、不产生费用**;若以后想升级成真正的 AI 大模型对话,需要接后端和 API Key,找 Claude 帮忙

---

## 三、文件地图

```
~/personal-homepage/
├── 操作手册.md               ← 本文件
├── src/
│   ├── data.ts               ← ⚙️ 个人信息(姓名/学校/bio/邮箱/社交链接/网易云歌曲ID)
│   ├── index.css             ← 配色主题、动画 keyframes
│   └── components/
│       ├── Hero.tsx          ← 关于我(名字星光效果在这)
│       ├── BentoHobbies.tsx  ← 个人爱好 Bento 网格
│       ├── SocialLinks.tsx   ← 社交连接 + 复制邮箱
│       ├── Feedback.tsx      ← 反馈板(Supabase 存数据库)
│       ├── DigitalTwin.tsx   ← 数字分身「小何」(规则问答聊天窗)
│       ├── NeuralCathedral.tsx  ← 3D 神经元教堂背景(Three.js,当前在用)
│       ├── NeuralBackground.tsx ← 2D 神经元背景(旧版,备用)
│       ├── SparklesText.tsx  ← 星光文字组件
│       ├── GlassCard.tsx     ← 玻璃卡片核心组件
│       └── ...
├── public/avatar.jpg         ← 头像(换照片覆盖此文件)
├── public/photos/            ← 摄影作品墙的图片(photo-N.jpg)
├── .env.local                ← Supabase 密钥(已被 gitignore,勿上传)
├── dist/                     ← 构建产物(部署/上传用这个)
└── ~/Desktop/打开主页.command ← 桌面快捷启动脚本
```

---

*由 Claude Code 维护 · 最后更新:2026-09-17*
